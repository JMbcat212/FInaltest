#ifndef COUNTCHAR_H
#define COUNTCHAR_H
#include <iostream>
#include <string>

using namespace std;

/** @brief Counts the characters in the string
 * @param string pName
 */
int countChar(string pName){
	return pName.length();
}

#endif
